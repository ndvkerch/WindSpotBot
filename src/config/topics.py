from typing import Dict


class TopicConfig:
    """Конфигурация тем для спотов."""
    SPOT_TOPICS: Dict[str, int] = {
        "Должанка": 0,  # Замени на реальный message_thread_id
        "Ейск": 0,
        "Анапа": 0,
    }
